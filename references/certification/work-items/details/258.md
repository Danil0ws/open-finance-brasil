# Issue #258: Adding igbeTownCode in payment

## 📋 Detalhes

- **ID**: #258
- **Status**: closed
- **Autor**: Marcelino Verissimo (@marcelino.k9999)
- **Criado em**: 15/06/2022, 13:38:03
- **Atualizado em**: 16/06/2022, 12:15:12
- **Fechado por**: Erick Domingues
- **Fechado em**: 16/06/2022, 12:15:12
- **Labels**: Bug, PHASE 3 :: V1
- **Atribuído a**: Alexandre Morais
- **Link**: [Abrir no GitLab](https://gitlab.com/raidiam-conformance/open-finance/certification/-/work_items/258)

## 📝 Descrição

## 1. What Happened and what were you expecting?

The test is adding the field "ibgeTownCode" in payment request (I'm not putting this field in json configuration). The test is failing because the payment is different from consent


## 2. Test Id

https://web.conformance.directory.openbankingbrasil.org.br/log-detail.html?log=MUMyYhrqtE6acnd&public=true

## 💬 Notas e Comentários (4)

### Comentário 1
**Autor**: Erick Domingues (@erick.domingues1)
**Criado em**: 16/06/2022, 12:15:11

We have fixed this with our latest merge https://gitlab.com/obb1/certification/-/merge_requests/689.

Please re-open the ticket if the problem persists.

---

### Comentário 2
**Autor**: Erick Domingues (@erick.domingues1)
**Criado em**: 15/06/2022, 19:25:04

Hi,

You are correct. We have been working on updating the tests do not require the user to provide the PIX Payments payload, however, it seems that we ended up feeding a field that is not unique for PIX Payments (like the remittanceInformation information).

We will work to just copy this field from the PIX-Consents to the PIX-Payments payload if present and just leave it blank if not present.

It's worth noting that this problem might be appearing on more than just this single test module so a more complete review would be required.

```
PaymentConsent:
      type: object
      description: Objeto contendo dados de pagamento para consentimento.
      required:
        - type
        - currency
        - amount
        - details
      properties:
        type:
          $ref: '#/components/schemas/EnumPaymentType'
        schedule:
          oneOf:
            - $ref: '#/components/schemas/Schedule'
        date:
          type: string
          format: date
          maxLength: 10
          pattern: '^(\d{4})-(1[0-2]|0?[1-9])-(3[01]|[12][0-9]|0?[1-9])$'
          example: '2021-01-01'
          description: |
            Mutuamente exclusivo com o objeto schedule. Este campo é obrigatório no caso de pagamento único. Neste caso, o objeto schedule não deve ser informado.
        currency:
          type: string
          maxLength: 3
          pattern: '^([A-Z]{3})$'
          example: BRL
          description: |
            Código da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
            Todos os valores monetários informados estão representados com a moeda vigente do Brasil.
        amount:
          type: string
          minLength: 4
          maxLength: 19
          pattern: '^((\d{1,16}\.\d{2}))$'
          example: '100000.12'
          description: |
            Valor da transação com 2 casas decimais.
        ibgeTownCode:
          type: string
          minLength: 7
          maxLength: 7
          pattern: '^\d{7}$'
          example: '5300108'
          description: |
            Traz o código da cidade segundo o IBGE (Instituto Brasileiro de Geografia e Estatística).
            Para o preenchimento deste campo, o Iniciador de Pagamentos deve seguir a orientação do arranjo da forma de pagamento.
            O preenchimento do campo tanto em pix/payments quanto /consents deve ser igual. Caso haja divergência dos valores, a instituição deve retornar HTTP 422 com o código de erro PAGAMENTO_DIVERGENTE_DO_CONSENTIMENTO.
            Este campo faz referência ao campo CodMun do arranjo Pix.
```

---

### Comentário 3
**Autor**: Erick Domingues (@erick.domingues1)
**Criado em**: 15/06/2022, 19:24:11
**Atualizado em**: 15/06/2022, 19:24:11

assigned to @AlexRaidiam

---

### Comentário 4
**Autor**: Erick Domingues (@erick.domingues1)
**Criado em**: 15/06/2022, 19:23:51

Hi,

You are correct. We have been working on updating the tests do not require the user to provide the PIX Payments payload, however, it seems that we ended up feeding a field that is not unique for PIX Payments (like the remittanceInformation information).

We will work to just copy this field from the PIX-Consents to the PIX-Payments payload if present and just leave it blank if not present.

```
PaymentConsent:
      type: object
      description: Objeto contendo dados de pagamento para consentimento.
      required:
        - type
        - currency
        - amount
        - details
      properties:
        type:
          $ref: '#/components/schemas/EnumPaymentType'
        schedule:
          oneOf:
            - $ref: '#/components/schemas/Schedule'
        date:
          type: string
          format: date
          maxLength: 10
          pattern: '^(\d{4})-(1[0-2]|0?[1-9])-(3[01]|[12][0-9]|0?[1-9])$'
          example: '2021-01-01'
          description: |
            Mutuamente exclusivo com o objeto schedule. Este campo é obrigatório no caso de pagamento único. Neste caso, o objeto schedule não deve ser informado.
        currency:
          type: string
          maxLength: 3
          pattern: '^([A-Z]{3})$'
          example: BRL
          description: |
            Código da moeda nacional segundo modelo ISO-4217, ou seja, 'BRL'.
            Todos os valores monetários informados estão representados com a moeda vigente do Brasil.
        amount:
          type: string
          minLength: 4
          maxLength: 19
          pattern: '^((\d{1,16}\.\d{2}))$'
          example: '100000.12'
          description: |
            Valor da transação com 2 casas decimais.
        ibgeTownCode:
          type: string
          minLength: 7
          maxLength: 7
          pattern: '^\d{7}$'
          example: '5300108'
          description: |
            Traz o código da cidade segundo o IBGE (Instituto Brasileiro de Geografia e Estatística).
            Para o preenchimento deste campo, o Iniciador de Pagamentos deve seguir a orientação do arranjo da forma de pagamento.
            O preenchimento do campo tanto em pix/payments quanto /consents deve ser igual. Caso haja divergência dos valores, a instituição deve retornar HTTP 422 com o código de erro PAGAMENTO_DIVERGENTE_DO_CONSENTIMENTO.
            Este campo faz referência ao campo CodMun do arranjo Pix.
```

---

## 📊 Metadados

| Campo | Valor |
|-------|-------|
| ID Único | 110184059 |
| Projeto | 26426113 |
| Upvotes | 0 |
| Downvotes | 0 |
| Comentários | 3 |

*Gerado em 16/09/2026, 15:30:40*
